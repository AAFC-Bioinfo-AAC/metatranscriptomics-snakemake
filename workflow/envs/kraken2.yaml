name: kraken2
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - kraken2=2.1.5 #version pinned to avoid issues with database compatibility
  - bracken=2.7.0
  - python=3.9
  - pandas